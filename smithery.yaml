# Smithery configuration for Freepik Seedream MCP
runtime: "typescript"

# Metadata
name: "freepik-seedream-mcp"
description: "MCP server for Freepik API with Seedream 4 text-to-image and image editing"
version: "1.0.0"

# Build settings
build:
  command: "npm run build"
